# 백준

## 1764

맵 유형의 문제다. 

defaultdict 를 활용해서 풀이했다. 듣도 못한 명단과 보도 못한 명단에 둘 다 있는 이름을 찾아야 한다. 듣도 못한 명단과 보도 못한 명단의 이름을 키로 하고 값으로 이름의 갯수를 세는 defaultdict 를 만든다.

각 명단에서 중복되는 이름은 없어서 defaultdict 의 값이 2인 키가 듣도 보도 못한 명단에 속하는 이름이다.



